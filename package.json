{
  "name": "newdex",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && npx @cloudflare/next-on-pages",
    "start": "next start",
    "deploy": "npx wrangler pages deploy .vercel/output/static --project-name=Newdex"
  },
  "dependencies": {
    "next": "^14.2.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@cloudflare/next-on-pages": "^1.13.0",
    "wrangler": "^3.0.0"
  }
}